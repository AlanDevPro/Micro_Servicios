<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Agregar Usuario</title>
  <style>
    body { font-family: Arial, sans-serif; margin:24px; background:#f4f6f8; }
    .card { max-width:600px; margin:auto; background:#fff; padding:20px; border-radius:10px; box-shadow:0 6px 20px rgba(0,0,0,.06); }
    label { display:block; margin-top:12px; font-weight:600; }
    input { width:100%; padding:10px; margin-top:6px; border-radius:8px; border:1px solid #ddd; }
    button { margin-top:14px; padding:10px 14px; border-radius:8px; background:#10b981; color:#fff; border:0; cursor:pointer; }
    .back { margin-right:10px; text-decoration:none; color:#374151; }
    .error { background:#fff5f5; border:1px solid #fecaca; padding:10px; border-radius:8px; color:#991b1b; margin-top:10px; }
  </style>
</head>
<body>
  <div class="card">
    <h2>Agregar Usuario</h2>
    <a class="back" href="/">← Volver</a>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/users">
      <label for="name">Nombre</label>
      <input id="name" name="name" type="text" required value="<%= form.name %>" />

      <label for="email">Correo Electrónico</label>
      <input id="email" name="email" type="email" required value="<%= form.email %>" />

      <button type="submit">Guardar</button>
    </form>
  </div>
</body>
</html>
